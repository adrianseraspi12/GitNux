<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.adrianseraspi.gitnux">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <!--TODO: fix layout, add firebase crashlytics, X-lint-->

    <application
        android:name=".GitNuxApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".login.LoginActivity">

            <intent-filter>

                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />

            </intent-filter>

            <intent-filter>

                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data
                    android:host="callback"
                    android:scheme="gitnux" />
            </intent-filter>
        </activity>
        <activity
            android:name=".home.HomeActivity"
            android:label="@string/title_activity_home"
            android:theme="@style/AppTheme.NavDrawer" />
        <activity
            android:name=".overview.OverviewActivity"
            android:label="@string/overview"
            android:parentActivityName=".home.HomeActivity" />
        <activity
            android:name=".listrepo.ListRepositoryActivity"
            android:label="@string/repositories"
            android:parentActivityName=".home.HomeActivity" />
        <activity
            android:name=".liststarred.ListStarredActivity"
            android:label="@string/stars"
            android:parentActivityName=".home.HomeActivity" />
        <activity
            android:name=".listpeople.ListPeopleActivity"
            android:label="@string/people"
            android:parentActivityName=".home.HomeActivity" />
        <activity android:name=".repo.RepositoryActivity">
        </activity>

        <activity android:name=".codeviewer.CodeViewerActivity" />
        <activity android:name=".about.GitNuxPreferenceActivity"
            android:theme="@style/SettingsTheme"
            android:parentActivityName=".home.HomeActivity"/>

    </application>

</manifest>